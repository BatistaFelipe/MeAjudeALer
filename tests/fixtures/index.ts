import { resolve } from "path";

export const PDF_OK = resolve(__dirname, "ok.pdf");
export const PDF_EMPTY = resolve(__dirname, "empty.pdf");
export const PDF_LARGE_SIZE = resolve(__dirname, "large_size.pdf");
export const NOT_PDF = resolve(__dirname, "not_pdf.txt");
